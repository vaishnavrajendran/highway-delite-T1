/* For running code in local */


Add your env's in both client and server.
Go to client/config/axios and rename it to localhost address.

>>For running frontend :
        cd client
        npm i
        npm run dev

>>For running backend instance:
        cd server
        npm i 
        npm run start